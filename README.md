✔ A register:

holds ONE value at a time
is always overwritten when new data arrives
is extremely fast because it’s inside CPU
is directly accessed (no searching)

aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa