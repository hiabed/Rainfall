#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int p(int input) {
    char buffer[76]; // 0x4c = 76 bytes

    fflush(stdout); // Ensure all output is flushed before reading input
    gets(buffer);

    void *ret = __builtin_return_address(0); // Get the return address of the current function
    if (((unsigned int)ret & 0xb0000000) == 0xb0000000) {
        printf("0x8048620\n");
        _exit(1);
    }
    puts(buffer);
    char *dup_str = strdup(buffer);

    return 0; // The original function does not explicitly return a value, assuming 0
}

int main() {
    p();
}